# Copy this file to secrets.toml and fill in your API key.
# On Streamlit Community Cloud, paste these values into the Secrets editor instead.

GEMINI_API_KEY = "paste-your-gemini-api-key-here"

# Optional: a passphrase guests must enter before they can use the app.
# Leave empty (or remove the line) to make the app fully public.
APP_PASSWORD = ""
